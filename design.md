# Design System — 트램 공사 구간 통합 관제 대시보드

> 베이스: Palantir Blueprint (Palantir의 오픈소스 React UI 툴킷 — 고밀도 데스크톱 인터페이스에 최적화된 디자인 언어)를 기본 토큰으로 삼고, 이 프로젝트의 실제 화면 구조(관제 뷰 / 분석 뷰 이중 구조)에 맞춰 다크 테마 적용 범위와 경보 색상 팔레트를 추가로 정의한다.

---

## 1. Visual Theme & Atmosphere

이 대시보드는 두 종류의 화면을 명확히 구분해서 사용한다. Blueprint 자체가 "다크 랜딩 / 라이트 문서 작업면"이라는 두 표면을 분리해서 제시하는데, 이 구조를 그대로 **관제 뷰(다크) / 분석 뷰(라이트)** 라는 이 프로젝트의 실제 화면 성격에 대응시킨다.

- **관제 뷰 (다크, `#111418`)**: 2-1 실시간 병목 경보 지도, 2-3 AMR 거점 배치 관제 — 관제 요원이 교대 근무 내내 띄워놓는 상시 화면. 정보를 "읽는" 것보다 "이상 신호를 즉각 포착"하는 것이 목적이라 어두운 배경 위에 경보색이 튀도록 구성한다.
- **분석 뷰 (라이트, `#ffffff` + `#1c2127`)**: 1-1/1-2/1-3 예측·네트워크·군집 분석, 공구별 리포트, Home — 차분히 읽고 검토하는 화면. Blueprint의 조밀한 문서 작업면 스타일을 그대로 따른다.

이 분리는 톤 자체가 "지금 당장 봐야 하는 화면인지, 차분히 검토하는 화면인지"를 사용자에게 미리 알려주는 역할도 한다.

---

## 2. Color Palette & Roles

### 2-1. 베이스 토큰 (Blueprint 원본)

| 역할 | 값 | 용도 |
| --- | --- | --- |
| Primary | `#2d72d2` | 버튼, 선택된 구간 하이라이트, 링크 액션 |
| Dark canvas | `#111418` | 관제 뷰 배경 |
| On-primary | `#ffffff` | 다크 배경 위 텍스트/아이콘 |
| Foreground | `#1c2127` | 라이트 배경 위 본문 텍스트 |
| Muted | `#5f6b7c` | 보조 설명, 타임스탬프, 캡션 |
| Link/accent | `#215db0` | 딥링크, 클릭 가능 요소, 카드 강조 텍스트 |
| Canvas | `#ffffff` | 분석 뷰 배경, 카드 배경 |

### 2-2. 경보 색상 팔레트 (신규 추가)

Blueprint 자체에는 상태/경보 색상 토큰이 없다. 이 프로젝트는 "정상/주의/심각" 3단계 판정이 화면 전반의 핵심 정보이므로, 국내 사용자에게 이미 학습된 지도 서비스(카카오맵/네이버맵)의 도로 정체 색채 문법을 별도 팔레트로 추가한다. 새로운 색채 규칙을 발명하지 않고 기존 관습을 따르는 것이 관제 화면에서는 이해 속도 면에서 더 유리하다.

| 상태 | 값 | 용도 |
| --- | --- | --- |
| 정상 | `#2f9e44` | 구간 색상, 상태 배지 |
| 주의 | `#f5b800` | 구간 색상, 상태 배지, 황색 경보 배너 |
| 심각 | `#e03131` | 구간 색상, 상태 배지, 적색 경보 배너 |
| 정상(다크 배경용) | `#3ecf6e` | 다크 캔버스 위에서 채도를 살짝 올린 버전 |
| 주의(다크 배경용) | `#ffd43b` | 〃 |
| 심각(다크 배경용) | `#ff6b6b` | 〃 |

다크 배경(`#111418`) 위에서는 원색을 그대로 쓰면 눈에 잘 안 띄므로, 다크 전용 보정 버전을 별도로 둔다. 라이트 배경(분석 뷰)에서는 기본값을 그대로 사용한다.

### 2-3. 색상 사용 원칙

- Primary(`#2d72d2`)는 "액션"에만 쓴다 — 상태 표현에는 절대 쓰지 않는다(경보 팔레트와 역할을 명확히 분리).
- 경보 3색은 오직 구간 상태·배지·경보 배너에만 쓴다. 장식적 용도로 사용하지 않는다.
- Palantir 원본 문서에 명시된 원칙 그대로, 확인되지 않은 색상(예: Palantir 기업 브랜드 컬러)은 가져오지 않는다 — 이 팔레트에 정의된 값만 사용한다.

---

## 3. Typography Rules

Blueprint는 특정 브랜드 서체를 지정하지 않고 시스템 폰트 스택을 그대로 쓴다. 이 프로젝트도 동일하게 시스템 폰트를 기본으로 하되, 한글 렌더링을 위해 스택 앞단에 한글 우선 폰트를 추가한다.

```
font-family: "Pretendard", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
```

| 역할 | 크기 | 굵기 | 줄높이 | 사용처 |
| --- | --- | --- | --- | --- |
| Landing/Hero title | 28px | 400 | 33.6px | Home 상단 타이틀 |
| Section title | 36px | 600 | 40px | 각 화면 최상단 헤딩 |
| Body | 16px | 400 | 24px | 본문, 표 내용 |
| Caption/Muted | 14px | 400 | 18px | 타임스탬프, 보조 설명, 카드 소텍스트 |

**신규 추가 — 경보 강조 텍스트**: 심각/주의 상태를 텍스트로 표기할 때는 Body 크기에 굵기 600을 적용해 시각적으로 눈에 띄게 한다(Blueprint 원본엔 이런 강조 규칙이 없어 프로젝트 특성상 추가).

---

## 4. Spacing & Radius

Blueprint의 조밀한 데스크톱 작업면 원칙을 그대로 따른다 — 여백을 넉넉하게 주는 마케팅 페이지 스타일이 아니라, 정보 밀도를 우선한다.

| 토큰 | 값 |
| --- | --- |
| spacing.xs | 4px |
| spacing.sm | 8px |
| spacing.md | 12px |
| spacing.lg | 16px |
| spacing.xl | 20px |
| radius.sharp | 0px (테이블, 지도 컨테이너) |
| radius.control | 4px (버튼, 카드, 입력 필드) |
| radius.round | 30px (경보 배지, 상태 pill에 한정 사용) |

---

## 5. Component Stylings

### 5-1. 카드 (Blueprint 원본 유지)

```
background: #ffffff (라이트) / #1a1e24 (다크, 신규 정의)
color: #215db0 (강조 텍스트)
border-radius: 4px
padding: 20px
box-shadow: rgba(0,0,0,0.15) 0 0 0 1px, rgba(0,0,0,0.02) 0 0 5px 0
```

- **다크 배경용 카드(`#1a1e24`)는 신규 정의**: Blueprint 원본은 다크 캔버스 위 카드 값을 제공하지 않으므로, 관제 뷰(2-1, 2-3)의 KPI 카드·팝업 패널에 쓸 값을 이 프로젝트에서 새로 정했다. 배경은 캔버스(`#111418`)보다 한 단계 밝게, 테두리는 낮은 대비의 옅은 회색선(`rgba(255,255,255,0.08)`)으로 처리한다.

### 5-2. 상태 배지 (신규 추가 — Grafana류 카드 패턴 참고)

Home, 공구별 리포트의 KPI 카드에는 Grafana식 "숫자 + 라벨 + 미니 트렌드" 패턴을 카드 컴포넌트 위에 얹는다. Blueprint 카드 지오메트리(4px radius, 20px padding)는 그대로 두고, 내부 구성만 아래처럼 확장한다.

```
[숫자, 32px bold] [라벨, 14px muted]
[미니 스파크라인 또는 상태 배지]
```

### 5-3. 지도 마커/팝업 (신규 추가 — Flightradar24류 실시간 트래킹 패턴 참고)

- 구간 색상은 경보 팔레트(2-2절)를 그대로 사용
- 클릭 시 팝업 카드는 5-1의 카드 스타일을 그대로 적용하되, 팝업 전용으로 padding을 12px(spacing.md)로 축소해 지도 위에서 과도하게 크지 않도록 조정
- 다수 마커가 겹칠 때는 클러스터링 처리(줌 레벨에 따라 공구 단위 롤업 → 구간 단위 드릴다운)

### 5-4. 버튼

```
기본(minimal): height 40px, padding 4px 16px, radius 4px
강조(primary): height 30px, padding 4px 8px, radius 4px, background #2d72d2, color #ffffff
```

---

## 6. Layout Principles

- 관제 뷰는 지도가 화면의 60% 이상을 차지하는 지도 중심 레이아웃을 기본으로 한다. 표·카드는 지도를 보조하는 사이드 패널로만 배치한다.
- 분석 뷰는 Blueprint의 조밀한 문서 작업면 원칙을 따라 16px 본문, 4px 카드/컨트롤 모서리, 4/8/12/16/20px 여백 체계를 유지한다.
- 내비게이션 행(list-item/menuitem)과 버튼의 시각적 구분을 명확히 유지한다 — 모든 클릭 가능 요소를 버튼처럼 스타일링하지 않는다.
- 반응형(모바일) 레이아웃은 이번 프로젝트 범위에서 정의하지 않는다. Blueprint 자체도 모바일 퍼스트가 아닌 데스크톱 작업면에 최적화되어 있으며, 이 대시보드도 관제 요원의 데스크톱 상시 사용을 전제로 한다.

---

## 7. Depth, Elevation & Effects

- 카드 그림자는 Blueprint 원본 값을 그대로 사용: `rgba(0,0,0,0.15) 0px 0px 0px 1px, rgba(0,0,0,0.02) 0px 0px 5px 0px`(라이트 배경 기준)
- 다크 배경에서는 그림자 대신 옅은 테두리선(`rgba(255,255,255,0.08)`)으로 카드 경계를 표현한다 — 어두운 배경 위 그림자는 시각적으로 거의 드러나지 않기 때문에 신규로 정한 규칙
- 모달/토스트/오버레이 스타일은 정의하지 않는다(Blueprint 원본에도 근거 없음). 필요 시 별도로 정의

---

## 8. Do's and Don'ts

### Do
- 정보 밀도를 우선하고, 불필요한 여백·장식으로 화면을 늘리지 않는다
- 경보색은 오직 상태 표현에만 쓰고, Primary색은 오직 액션에만 쓴다
- 카드·버튼의 지오메트리(4px radius, 정의된 padding)를 전 화면에서 일관되게 유지한다
- 관제 뷰(다크)와 분석 뷰(라이트)의 배경 전환으로 화면 성격을 구분한다

### Don't
- 시스템 폰트 스택을 임의의 브랜드 서체로 대체하지 않는다
- 관제 뷰에 마케팅 페이지 스타일의 큰 히어로 섹션이나 장식적 애니메이션을 넣지 않는다
- 검증되지 않은 hover/focus/pressed 상태 스타일을 임의로 만들어 넣지 않는다 — 필요한 시점에 실제 사용성 테스트를 거쳐 추가한다
- 정상/주의/심각 3색 외의 임의 색상으로 상태를 표현하지 않는다(색이 늘어나면 관제 요원의 즉각적 판단 속도가 떨어짐)

---

## 9. Motion & Easing

- 기본적으로 모션은 최소화한다. 관제 화면 특성상 애니메이션이 실제 경보를 놓치게 만들 위험이 있다
- 예외: 신규 경보 발생 시에만 짧은(200ms 이하) 페이드/펄스 효과로 주의를 환기하는 것은 허용
- 그 외 페이지 전환, 카드 호버 등은 즉시 반응(트랜지션 없음)을 기본으로 한다

---

## 10. Agent Prompt (구현 시 참고용 요약)

관제 뷰(2-1, 2-3)는 다크 배경(`#111418`)에 지도 중심 레이아웃으로, 경보 팔레트(정상 `#3ecf6e`/주의 `#ffd43b`/심각 `#ff6b6b`, 다크 배경용 보정치)를 사용해 구성한다. 분석 뷰(1-1~1-3, Home, 공구별 리포트)는 라이트 배경(`#ffffff`, 텍스트 `#1c2127`)에 Blueprint의 조밀한 문서 작업면 스타일(16px 본문, 4px 카드/컨트롤 모서리, 4/8/12/16/20px 여백)을 따른다. Primary 액션은 `#2d72d2`로 통일하고 상태 표현에는 사용하지 않는다. 카드는 4px radius, 20px padding, 라이트에서는 옅은 그림자, 다크에서는 옅은 테두리선을 사용한다. 반응형·모달·hover 상태 등 미검증 영역은 임의로 채우지 않는다.

---
**베이스**: Palantir Blueprint (https://blueprintjs.com/) — Apache 2.0
**프로젝트 추가 정의**: 경보 색상 팔레트, 다크 배경용 카드/그림자 보정, 지도 마커/팝업 패턴, KPI 카드 패턴, 모션 원칙